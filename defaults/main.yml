---
# defaults file for lksnyder0.logstash
ls_version: 7.5.2

el_java_package: java-11-openjdk-devel
debian_java_package: openjdk-11-jre-headless

ls_service_enable: true

service_manager: systemd
# service_manager: service

# logstash.yml settings
# Node Identity
ls_node_name: "{{ ansible_hostname }}"
